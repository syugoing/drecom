<%= form_for(@mission, url: choose_new_or_edit) do |f| %>
  <%= f.error_messages! %>
  <p>タイトル</p>
  <%= f.text_field :title, class: 'form-control' %><br>
  <p>目標</p>
  <%= f.text_field :content, class: 'form-control' %><br>
  <%= f.submit '投稿', class: 'btn btn-primary btn-block' %>
<% end %>
